name: 10 - TSE

on:
  schedule:
    # Espaçado para 3 vezes ao dia após o encerramento do prazo de registro (15/08).
    # Roda 7 minutos antes de 14 - Planos de Governo (05:15, 11:15 e 17:15 BRT).
    - cron: '15 8 * * *'   # 05:15 BRT
    - cron: '15 14 * * *'  # 11:15 BRT
    - cron: '15 20 * * *'  # 17:15 BRT
  workflow_dispatch:

# Rodada agendada atrasa (o Actions enfileira de 25 a 250 min) e pode alcançar a
# da hora seguinte. Duas gravando a mesma aba é o 409 do Sheets que em 06/08/2026
# deixou a aba de candidaturas vazia no meio da rodada. A fila resolve na origem;
# a corrente não é cancelada para a coleta em andamento terminar de gravar.
concurrency:
  group: tse-candidaturas
  cancel-in-progress: false

jobs:
  coletar:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      # pyarrow: o fim da rodada republica o cache Parquet das abas que ela escreve
      - run: pip install gspread requests pandas curl_cffi pyarrow

      - name: Escrever credentials.json
        run: printf '%s' "$GOOGLE_CREDENTIALS_JSON" > credentials.json
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}

      - run: python -m outros.tse_candidaturas
        env:
          SPREADSHEET_ID_TSE: ${{ secrets.SPREADSHEET_ID_TSE }}
          # Planilha de trabalho da equipe ("Eleições 2026 - Nomes competitivos"),
          # onde a aba de candidatos a deputado federal é reescrita a cada rodada,
          # e a da legislatura atual (deputados_completo_assPEC), de onde sai a
          # marcação de reeleição. Faltando qualquer um dos dois, a exportação
          # avisa no log e a coleta segue normal.
          SPREADSHEET_ID_COMPETITIVOS: ${{ secrets.SPREADSHEET_ID_COMPETITIVOS }}
          SPREADSHEET_ID_DEPUTADOS_ATUAIS: ${{ secrets.SPREADSHEET_ID_DEPUTADOS_ATUAIS }}
